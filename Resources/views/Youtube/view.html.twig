{#

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

https://github.com/sonata-project/SonataMediaBundle/blob/master/Resources/views/Provider/view_youtube.html.twig

#}

<object width="{{ options.width }}" height="{{ options.height }}">
    <param name="movie" value="http://www.youtube.com/v/{{ media.metadata.uuid }}?{{ options.player_parameters }}"></param>
    <param name="allowFullScreen" value="{{ options.allowFullScreen }}"></param>
    <param name="allowScriptAccess" value="{{ options.allowScriptAccess }}"></param>

    <embed
        type="application/x-shockwave-flash"
        src="http://www.youtube.com/v/{{ media.metadata.uuid }}?{{ options.player_parameters }}"
        width="{{ options.width }}"
        height="{{ options.height }}"
        allowfullscreen="{{ options.allowFullScreen }}"
        allowscriptaccess="{{ options.allowScriptAccess }}">
    </embed>
</object>